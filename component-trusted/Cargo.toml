[package]
name = "component-trusted"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.41"

[package.metadata.component]
package = "sandbox:trusted"

[package.metadata.component.bindings]
# Generate bindings from the WIT file
